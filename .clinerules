# NRP 项目工作流规则

## 任务开始前

在开始任何开发任务之前，大模型必须先读取 `PROJECT_REVIEW.md` 文件以熟悉项目情况，了解：
- 项目技术栈和结构
- 现有功能和待开发功能
- 路由配置和状态管理
- API 请求工具的使用方式

**如果修改的是已有页面**，还必须先读取该页面的 PRD 文件（文件路径：`@/pages/页面目录/页面文件名_prd.md`），熟悉该页面的：
- 页面功能概述
- 核心功能（列表功能、筛选功能、操作功能）
- 交互设计
- API 接口
- 验收标准

## 任务结束后

在完成每个开发任务后，大模型必须更新 `PROJECT_REVIEW.md` 文件，记录以下变更：
- 新增的页面组件或功能模块
- 新增的路由配置
- 新增的工具函数或状态管理
- 现有的待开发功能清单
- 其他项目结构的变化

**同时需要维护对应页面的 PRD 文件**：
- 如果修改的是已有页面：更新该页面 PRD 文件中的相关内容（功能变更、接口变更等）
- 如果新增的是新页面：创建该页面的 PRD 文件（文件路径：`@/pages/页面目录/页面文件名_prd.md`）
- PRD 文件命名规则：**页面代码文件名称 + _prd**（例如：`ProjectSaleList_prd.md`）

## 开发规范

1. **组件开发**: 使用 TypeScript + React Hooks
2. **样式**: 使用 Less 预处理器
3. **UI 组件**: 使用 Ant Design 6.x
4. **状态管理**: 使用 Zustand
5. **API 请求**: 使用 `@/utils/request` 中的封装方法

## 文件路径规范

- 组件路径: `@/components/` 
- 页面路径: `@/pages/`
- 工具函数: `@/utils/`
- 状态管理: `@/store/`
- 路由配置: `@/router/`
